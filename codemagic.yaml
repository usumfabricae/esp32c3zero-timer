workflows:
  android-capacitor-workflow:
    name: Android Capacitor Build
    max_build_duration: 60
    instance_type: mac_mini_m1
    
    environment:
      vars:
        PACKAGE_NAME: "net.superfede.BleTimerWebView"
        APP_NAME: "BLE Timer"
      node: 22
      java: 21
      
    triggering:
      events:
        - push
        - tag
        - pull_request
      branch_patterns:
        - pattern: 'main'
          include: true
          source: true
  
  android-peripheral-demo:
    name: Android BLE Peripheral Demo
    max_build_duration: 60
    instance_type: mac_mini_m1
    
    environment:
      vars:
        PACKAGE_NAME: "net.superfede.BlePeripheralDemo"
        APP_NAME: "BLE Peripheral Demo"
      node: 22
      java: 21
      
    triggering:
      events:
        - push
        - tag
        - pull_request
      branch_patterns:
        - pattern: 'reverse'
          include: true
          source: true
        - pattern: 'peripheral-demo'
          include: true
          source: true
      
    scripts:
      - name: Install npm dependencies
        script: |
          cd webclient
          npm ci
          
          # Verify BLE Peripheral plugin was installed
          if [ -d "node_modules/@superfede/capacitor-ble-peripheral" ]; then
            echo "✓ BLE Peripheral plugin installed"
            
            # Verify it has the capacitor field
            if grep -q '"capacitor"' node_modules/@superfede/capacitor-ble-peripheral/package.json; then
              echo "✓ Plugin has capacitor configuration"
            else
              echo "✗ Plugin missing capacitor configuration!"
              exit 1
            fi
          else
            echo "✗ BLE Peripheral plugin NOT installed!"
            echo "Checking node_modules:"
            ls -la node_modules/@superfede/ || echo "No @superfede folder"
            exit 1
          fi
          

      - name: Verify Plugin Structure
        script: |
          cd webclient
          echo "=== Checking plugin files ==="
          ls -la ble-peripheral-plugin/src/
          echo ""
          echo "=== Checking plugin android folder ==="
          ls -la ble-peripheral-plugin/android/
          echo ""
          echo "=== Checking installed plugin ==="
          ls -la node_modules/@superfede/capacitor-ble-peripheral/
          echo ""
          echo "=== Checking installed plugin android folder ==="
          ls -laR node_modules/@superfede/capacitor-ble-peripheral/android/ || echo "Android folder not found!"
          echo ""
          echo "=== Checking package.json ==="
          cat node_modules/@superfede/capacitor-ble-peripheral/package.json
          
      - name: Build Main Web App
        script: |
          cd webclient
          CAPACITOR=true npm run build
          echo "✓ Main web app built"
          ls -la dist/
          
      - name: Setup Android Platform
        script: |
          cd webclient
          
          # Add Android platform (needs dist/ folder to exist)
          if [ ! -d "android" ]; then
            echo "Adding Android platform..."
            npx cap add android
          else
            echo "Android platform already exists"
          fi
          
          # Sync to ensure everything is up to date
          npx cap sync android
          
          echo "✓ Android platform ready"
          ls -la android/
      
      - name: Build Peripheral Demo Web App
        script: |
          cd webclient
          CAPACITOR=true npm run build:peripheral
          
          # Rename index-peripheral.html to index.html for Capacitor
          if [ -f "dist-peripheral/index-peripheral.html" ]; then
            mv dist-peripheral/index-peripheral.html dist-peripheral/index.html
            echo "✓ Renamed index-peripheral.html to index.html"
          fi
          
          echo "✓ Peripheral web app built"
          ls -la dist-peripheral/
          
      - name: Build Peripheral Demo APK
        script: |
          cd webclient
          
          # Clean the Android assets folder completely
          echo "Cleaning Android assets..."
          rm -rf android/app/src/main/assets/public/*
          
          # Backup current dist
          if [ -d "dist" ]; then
            mv dist dist-backup
          fi
          
          # Use peripheral dist
          mv dist-peripheral dist
          
          # Verify dist has index.html
          if [ ! -f "dist/index.html" ]; then
            echo "✗ Error: dist/index.html not found!"
            ls -la dist/
            exit 1
          fi
          
          # Force clean sync to ensure plugin is registered
          echo "Cleaning Capacitor cache..."
          rm -rf android/capacitor-cordova-android-plugins
          
          # Sync Capacitor to copy new web assets and register plugins
          echo "Syncing peripheral demo assets and plugins..."
          npx cap sync android
          
          # Verify plugin was registered
          echo "Checking plugin registration..."
          if [ -f "android/capacitor-cordova-android-plugins/build.gradle" ]; then
            echo "✓ Plugin bridge created"
            cat android/capacitor-cordova-android-plugins/build.gradle
          fi
          
          # Check if plugin is in settings.gradle
          if grep -q "capacitor-ble-peripheral" android/settings.gradle; then
            echo "✓ BLE Peripheral plugin found in settings.gradle"
          else
            echo "✗ BLE Peripheral plugin NOT in settings.gradle"
            echo "Content of settings.gradle:"
            cat android/settings.gradle
          fi
          
          # Verify the correct files were copied
          echo "Verifying assets..."
          if [ -f "android/app/src/main/assets/public/index.html" ]; then
            if grep -q "BLE Peripheral Demo" android/app/src/main/assets/public/index.html; then
              echo "✓ Correct assets copied (Peripheral Demo)"
            else
              echo "✗ Wrong assets! Still has main app"
              echo "Content of index.html:"
              cat android/app/src/main/assets/public/index.html
              exit 1
            fi
          else
            echo "✗ index.html not found in assets!"
            ls -la android/app/src/main/assets/public/
            exit 1
          fi
          
          # Clean build to avoid Gradle cache issues
          echo "Cleaning Gradle build..."
          cd android
          chmod +x gradlew
          ./gradlew clean
          
          # Build APK
          echo "Building APK..."
          ./gradlew assembleDebug
          cd ..
          
          # Copy APK with new name
          mkdir -p ../build-output
          cp android/app/build/outputs/apk/debug/app-debug.apk ../build-output/ble-peripheral-demo-debug.apk
          
          # Restore original dist
          mv dist dist-peripheral
          
          # Restore index.html to index-peripheral.html in dist-peripheral
          if [ -f "dist-peripheral/index.html" ]; then
            mv dist-peripheral/index.html dist-peripheral/index-peripheral.html
          fi
          
          if [ -d "dist-backup" ]; then
            mv dist-backup dist
          fi
          
          cd ..
          echo "✓ Peripheral demo APK built successfully"
          

          
    artifacts:
      - build-output/*.apk
      
    publishing:
      email:
        recipients:
          - your-email@example.com
        notify:
          success: true
          failure: true
