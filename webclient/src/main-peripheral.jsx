import React from 'react'
import ReactDOM from 'react-dom/client'
import AppPeripheralDemo from './AppPeripheralDemo.jsx'
import './styles/global.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppPeripheralDemo />
  </React.StrictMode>,
)
